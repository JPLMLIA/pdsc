# For more information about setting up environment YAML files for conda, see:
# https://conda.io/docs/user-guide/tasks/manage-environments.html#create-env-file-manually

# bare bones conda environment to run osprey 

name: p37

channels:
  - conda-forge
  - menpo
  - defaults

dependencies:
  - python=3.7
